import 'package:flutter/material.dart';
import 'package:mobile_app/views/login_screen.dart';

/// Legacy alias preserving full compatibility with GatewayLoginScreen references
/// while rendering the finalized Google Stitch Entry Gateway Light Breathable design.
class GatewayLoginScreen extends StatelessWidget {
  const GatewayLoginScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const LoginScreen();
  }
}
